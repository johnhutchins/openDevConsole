# To use locally (extension in review for chrome app store):
1. open chrome extensions local (chrome://extensions)
2. turn developer mode on
3. load unpacked --> add folder that the above files are within.
